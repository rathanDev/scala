/**
 * Challenge urself
 */
package jana.tasteOfScala;

/**
  * Challenge urself
  */
//        object Upper {
//          def up(strings: String*) = strings.map(_.toUpperCase())
//
//          def up2(strings:String*) = {
//            strings.map(_.toUpperCase()).foreach(printf("%s ", _))
//          }
//
//          def main(args:Array[String]): Unit = {
//            args.map(_.toUpperCase()).foreach(printf("%s ", _))
//          }

//        }
